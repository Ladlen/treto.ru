<?php

/**
 * Данные конфигурации.
 */

$config = [
    // Файл с изображениями.
    'brickworkImages' => [
        'dimensions' => [
            // Высота изображения.
            'height' => 200,    // px
            // Кромка вокруг изображения. Обеспечивает расстояние между изображениями.
            'margin' => 2       // px
        ],
        // Файл со списком изображений.
        'file' => [
            // Конец строки.
            'EOL' => "\r\n"
        ],
    ],
    // Логирование.
    'log' => [
        // Куда отправлять ошибку (см. ф-ю error_log()).
        'type' => 3,
        // Назначение (см. ф-ю error_log()).
        'destination' => APP_DIR . 'runtime/logs/app.log',
    ],
    // Отладочный режим.
    'debug' => true
];

return $config;